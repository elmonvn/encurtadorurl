# Utilizando root/P@assw0rd como usuário/senha
version: '3.1'

services:

  mongo:
    image: mongo
    restart: always
    environment:
            #MONGO_INITDB_ROOT_USERNAME: root
            #MONGO_INITDB_ROOT_PASSWORD: 'P@ssw0rd'
      MONGO_INITDB_DATABASE: encurtadorUrl
